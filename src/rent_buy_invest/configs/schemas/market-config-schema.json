{
  "title": "Market Configuration",
  "description": "Config file relating to assumptions about the 'market'.",
  "type": "object",
  "properties": {
    "market_rate_of_return": {
      "description": "ANNUAL rate of return in the market, as a fraction",
      "type": "number"
    },
    "tax_brackets_inflation": {
      "description": "Rate at which the tax bracket limits inflate (by government policy)",
      "type": "number"
    },
    "annual_inflation_rate": {
      "description": "General ANNUAL rate of price inflation in the economy, as a fraction. Used to grow the home costs that track prices rather than home value: utilities, HOA fees, homeowners and flood insurance, and the home warranty. This is not the rate rents rise at, which is 'annual_rent_inflation_rate' in the rent config, and it is not the rate home values rise at, which is 'annual_home_appreciation_rate' in the buy config.",
      "type": "number"
    },
    "tax_brackets": {
      "description": "Tax brackets",
      "type": "object",
      "properties": {
        "ordinary_income_tax_brackets": {
          "description": "Ordinary income tax (also short term capital gains tax) brackets. List of tax brackets ordered from lowest bracket to highest bracket. Each bracket has a lower limit, upper limit, and marginal tax rate. The first bracket's lower limit is assumed to be 0, and every other bracket's lower limit is equal to its previous bracket's upper limit. The last bracket's upper limit must be infinity.",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "upper_limit": {
                "description": "The upper limit of this tax bracket.",
                "type": "number"
              },
              "tax_rate": {
                "description": "The marginal tax rate for this tax bracket.",
                "type": "number"
              }
            },
            "required": ["upper_limit", "tax_rate"]
          }
        },
        "long_term_capital_gains_tax_brackets": {
          "description": "Long term capital gains tax brackets. List of tax brackets ordered from lowest bracket to highest bracket. Each bracket has a lower limit, upper limit, and marginal tax rate. The first bracket's lower limit is assumed to be 0, and every other bracket's lower limit is equal to its previous bracket's upper limit. The last bracket's upper limit must be infinity.",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "upper_limit": {
                "description": "The upper limit of this tax bracket.",
                "type": "number"
              },
              "tax_rate": {
                "description": "The marginal tax rate for this tax bracket.",
                "type": "number"
              }
            },
            "required": ["upper_limit", "tax_rate"]
          }
        }
      },
      "required": ["ordinary_income_tax_brackets", "long_term_capital_gains_tax_brackets"]
    }
  },
  "required": ["market_rate_of_return", "tax_brackets_inflation", "annual_inflation_rate", "tax_brackets"]
}